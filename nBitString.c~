#include<stdio.h>
#include<string.h>
#define N 10
char a[N];
binary(int n)
{
	if(n<1)
	{
		printf("%s\n",a);
		if(strcmp(a,"a")==0)
		getchar();
	}
	else
	{
		a[n-1]=0;
		binary(n-1);
		a[n-1]=1;
		binary(n-1);
	}
}

int main()
{
	binary(N);
	return 0;	
}
